node_modules/
dist/
.DS_Store
*.log
.env
.env.local
.env.*
!.env.example
.env.*.local

# Runtime workspace — regenerable outputs per episode
projects/

# User content drop zone — bulky media
music_library/

# Sibling-repo bridge — DO NOT publish.
# Contains references to a separate reference codebase used during the initial
# clean-room build. Removed before flipping this repo public.
.migration/

# Alpha-loop ephemeral data (not shared)
.alpha-loop/sessions/
.alpha-loop/auth/
.alpha-loop/traces/
.alpha-loop/templates/*.bak
.worktrees/

# Music Video Compose Director

For the bundled zero-key sample, produce a lightweight render report and MP4 preview without paid tools. The purpose is to prove the project, starter, artifact, and export loop works on a fresh machine.

The canonical compose artifact is `render_report.json`. The reported output path must point to a file written under the episode workspace.

# Music Video Edit Director

For the bundled zero-key sample, cut a simple beat-synced timeline against the generated starter visual. Keep the sample short, deterministic, and suitable for NLE export.

The canonical edit artifact is `edit_decisions.json`. Cuts must have positive duration and stay within the render duration.
